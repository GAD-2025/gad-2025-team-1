import React from 'react';
import Header from '../components/Header';
import './Setting.css';

const Setting = () => {
    return (
        <div className="setting-page">
            <Header />
            <main className="setting-container">
                <h1>설정 페이지</h1>
                <p>이 페이지는 현재 비어 있습니다.</p>
            </main>
        </div>
    );
};

export default Setting;
